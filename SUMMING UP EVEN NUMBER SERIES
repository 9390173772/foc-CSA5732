#include<stdio.h>
int main()
{
	int x=0,i,n;
	printf("enter the number:");
	scanf("%d",&n);
	for(i=2;i<=n;i++)
	{
		if(i%2==0)
		x=x+i;
    }
		printf("the sum of even number is: %d",x);
		return 0;
}
